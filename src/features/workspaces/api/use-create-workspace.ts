import { Mutation } from "@tanstack/react-query"
import {interResponseType} from  "hono"

type ResponsType = InterResponeType

export const useCreateWorkspace = () =>{
    const mutate = Mutation({

    })
}